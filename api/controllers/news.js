const mongoose = require('mongoose');

module.exports = {
  getNews: (req, res) => {},

  newNews: (req, res) => {},

  updateNews: (req, res) => {
    const id = req.params.id;
  },

  deleteNews: (req, res) => {
    const id = req.params.id;
  }
}